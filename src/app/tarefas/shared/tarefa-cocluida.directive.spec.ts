import { TarefaCocluidaDirective } from './tarefa-cocluida.directive';

describe('TarefaCocluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new TarefaCocluidaDirective();
    expect(directive).toBeTruthy();
  });
});
